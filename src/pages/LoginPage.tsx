import * as React from "react";
import Authform from "../components/AuthForm";

const LoginPage = () => {
  return (
    <div>
      <h1>Login</h1>
      <Authform />
    </div>
  );
};

export default LoginPage;
